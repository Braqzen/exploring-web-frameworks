# Shared configuration for services
x-common: &common-setup
  deploy:
    resources:
      limits:
        memory: 128M
  memswap_limit: 128M
  restart: unless-stopped

x-common-env-telemetry: &common-env-telemetry
  OTEL_EXPORTER_OTLP_ENDPOINT: http://telemetry-alloy:4318
  OTEL_METRIC_EXPORT_INTERVAL: "1000"
  PYROSCOPE_URL: http://telemetry-alloy:9999

x-common-env-socket: &common-env-socket
  SOCKET: 0.0.0.0:8000

services:
  generator:
    image: servers-generator:latest
    <<: *common-setup
    environment:
      RUST_LOG: info
      <<: *common-env-telemetry
    depends_on:
      - telemetry-alloy

  rust-axum:
    image: servers-axum:latest
    <<: *common-setup
    environment:
      RUST_LOG: info
      <<: [*common-env-socket, *common-env-telemetry]

  rust-actix:
    image: servers-actix:latest
    <<: *common-setup
    environment:
      RUST_LOG: info
      <<: [*common-env-socket, *common-env-telemetry]

  rust-warp:
    image: servers-warp:latest
    <<: *common-setup
    environment:
      RUST_LOG: info
      <<: [*common-env-socket, *common-env-telemetry]

  rust-rocket:
    image: servers-rocket:latest
    <<: *common-setup
    environment:
      RUST_LOG: info
      ROCKET_CLI_COLORS: "false"
      <<: [*common-env-socket, *common-env-telemetry]

  rust-poem:
    image: servers-poem:latest
    <<: *common-setup
    environment:
      RUST_LOG: info
      <<: [*common-env-socket, *common-env-telemetry]

  rust-salvo:
    image: servers-salvo:latest
    <<: *common-setup
    environment:
      RUST_LOG: info
      <<: [*common-env-socket, *common-env-telemetry]

  typescript-express:
    image: servers-express:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  typescript-fastify:
    image: servers-fastify:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  typescript-hono:
    image: servers-hono:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  typescript-koa:
    image: servers-koa:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  typescript-elysia:
    image: servers-elysia:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  python-flask:
    image: servers-flask:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  python-fastapi:
    image: servers-fastapi:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  python-sanic:
    image: servers-sanic:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  python-quart:
    image: servers-quart:latest
    <<: *common-setup
    environment:
      LOG_LEVEL: info
      <<: [*common-env-socket, *common-env-telemetry]

  telemetry-grafana:
    image: grafana/grafana:12.2
    ports:
      - "3000:3000"
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Admin
      GF_ANALYTICS_REPORTING_ENABLED: "false"
      GF_ANALYTICS_CHECK_FOR_UPDATES: "false"
      GF_ANALYTICS_CHECK_FOR_PLUGIN_UPDATES: "false"
      GF_ANALYTICS_FEEDBACK_LINKS_ENABLED: "false"
      GF_PLUGINS_PUBLIC_KEY_RETRIEVAL_DISABLED: "true"
      GF_PLUGINS_PREINSTALL_DISABLED: "true"
      GF_NEWS_NEWS_FEED_ENABLED: "false"
    volumes:
      - ../telemetry/grafana/provisioning:/etc/grafana/provisioning
      - grafana-data-volume:/var/lib/grafana
    depends_on:
      - telemetry-loki
      - telemetry-pyroscope
      - telemetry-prometheus
      - telemetry-tempo
    restart: unless-stopped

  telemetry-prometheus:
    image: prom/prometheus:v3.5.4
    ports:
      - "9090:9090"
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --web.enable-remote-write-receiver
    volumes:
      - ../telemetry/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data-volume:/prometheus
    restart: unless-stopped

  telemetry-loki:
    image: grafana/loki:3.7
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/loki.yaml
    volumes:
      - ../telemetry/loki/loki.yaml:/etc/loki/loki.yaml
      - loki-data-volume:/loki
    restart: unless-stopped

  telemetry-pyroscope:
    image: grafana/pyroscope:2.0.4
    ports:
      - "4040:4040"
    command: -config.file=/etc/pyroscope/pyroscope.yaml
    volumes:
      - ../telemetry/pyroscope/pyroscope.yaml:/etc/pyroscope/pyroscope.yaml
      - pyroscope-data-volume:/data
    restart: unless-stopped

  telemetry-tempo:
    image: grafana/tempo:3.0.2
    ports:
      - "3200:3200"
    command: -config.file=/etc/tempo/tempo.yaml
    volumes:
      - ../telemetry/tempo/tempo.yaml:/etc/tempo/tempo.yaml
      - tempo-data-volume:/var/tempo
    restart: unless-stopped

  telemetry-alloy:
    image: grafana/alloy:v1.17.0
    volumes:
      - ../telemetry/grafana/alloy:/etc/alloy
      - alloy-data-volume:/var/lib/alloy/data
    command: run --disable-reporting /etc/alloy/config.alloy
    depends_on:
      - telemetry-loki
      - telemetry-tempo
      - telemetry-pyroscope
      - telemetry-prometheus
    restart: unless-stopped

volumes:
  grafana-data-volume:
  loki-data-volume:
  prometheus-data-volume:
  pyroscope-data-volume:
  tempo-data-volume:
  alloy-data-volume:
